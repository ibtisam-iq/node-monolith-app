# Copy this file to .env and fill in the values.
# Never commit .env to version control.

# MySQL credentials (must match compose.yml environment)
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=
DB_HOST=db

# Server port
PORT=5000
